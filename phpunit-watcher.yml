watch:
  directories:
    - src
    - tests
  fileMask: '*.php'
notifications:
  passingTests: true
  failingTests: true
phpunit:
  binaryPath: vendor/bin/phpunit
  arguments: '--stop-on-failure --testdox'
  timeout: 180
